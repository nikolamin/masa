"""Local utilities package for MASA.

Ensures imports like `from utils.featGen import FeatureProcesser` resolve to the
project's own utilities rather than any third-party `utils` modules that might
be present in the Python environment.
"""

